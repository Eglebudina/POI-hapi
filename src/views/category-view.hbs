{{> menu}}

<section class="section columns is-vcentered">
  <div class="column has-text-centered">
    <div class="title">
      {{category.name}}
    </div>
     {{> list-places}}
     {{> add-place}}
  </div>
  <div class="column">
    {{> category-image }}
  </div>
</section>

